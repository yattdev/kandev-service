# ── Secrets ───────────────────────────────────────────────────────────────────
# Never commit .env — it contains API tokens.
.env

# Never commit host.env — it holds this machine's real hosts/users/IPs.
# Share host.env.example instead. See host.env.example for the format.
host.env

# ── Editor / OS noise ─────────────────────────────────────────────────────────
.DS_Store
__pycache__/
*.py[cod]
.local-hotfix/*
!.local-hotfix/README.md
*.swp
*~

# ── Local scratch / notes (not for the public repo) ───────────────────────────
file.txt
